// Arrow Function
// While passing variaible into the function during definition is called parameters
const addNumbers = (a,b)=>{
    return (a+b)
}
// While passing the values to the function during calling is called arguments
console.log(addNumbers(10,40));


const subNumbers = (a,b)=> a-b

console.log(subNumbers(30,20));

